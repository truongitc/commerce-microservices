spring:
  application:
    name: product
  profiles:
    active: native
  cloud:
    config:
      uri: http://localhost:8001
      fail-fast: true

management:
  security:
    enabled: false

endpoints:
  actuator:
    enabled: true